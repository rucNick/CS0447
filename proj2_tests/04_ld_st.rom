v2.0 raw
# to load this file into Logisim:
# 1. use the poke tool and right-click the ROM component
# 2. select Load Image menu option
# 3. choose this file
040A40	# (PC=  0) li    r1, 5
040C80	# (PC=  1) li    r2, 6
040EC0	# (PC=  2) li    r3, 7
0C0001	# (PC=  3) st    [r0], r1
0C0202	# (PC=  4) st    [r0 + 1], r2
0C0403	# (PC=  5) st    [r0 + 2], r3
0A0100	# (PC=  6) ld    r4, [r0]
0A0340	# (PC=  7) ld    r5, [r0 + 1]
0A0580	# (PC=  8) ld    r6, [r0 + 2]
020020	# (PC=  9) put   r4
020028	# (PC= 10) put   r5
020030	# (PC= 11) put   r6
000000	# (PC= 12) hlt   
