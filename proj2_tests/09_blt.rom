v2.0 raw
# to load this file into Logisim:
# 1. use the poke tool and right-click the ROM component
# 2. select Load Image menu option
# 3. choose this file
042040	# (PC=  0) li    r1, 0x10
042C80	# (PC=  1) li    r2, 0x16
020008	# (PC=  2) put   r1
1E0248	# (PC=  3) adi   r1, r1, 1
2FFC0A	# (PC=  4) blt   r1, r2, top
000000	# (PC=  5) hlt   
